name: california_energy_architecture_2026
version: '1.0'
config-version: 2
profile: california_energy_architecture_2026
source-paths: ["models"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]
data-paths: ["data"]
test-paths: ["tests"]
seed-paths: ["seeds"]
target-path: "target"
clean-targets: ["target", "dbt_modules", "dbt_packages"]
models:
  california_energy_architecture_2026:
    staging:
      +materialized: view
    marts:
      +materialized: table
    utility:
      +materialized: view
seeds:
  california_energy_architecture_2026:
    example_seed:
      quote_columns: false
      header: true
